import type { Metadata } from "next";

export const metadata: Metadata = { title: "Disciplinary" };

export default function DisciplinaryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
