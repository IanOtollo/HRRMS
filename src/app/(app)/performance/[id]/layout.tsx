import type { Metadata } from "next";

export const metadata: Metadata = { title: "Appraisal" };

export default function AppraisalDetailLayout({ children }: { children: React.ReactNode }) {
  return children;
}
