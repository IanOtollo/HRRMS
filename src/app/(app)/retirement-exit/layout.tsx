import type { Metadata } from "next";

export const metadata: Metadata = { title: "Retirement & Exit" };

export default function RetirementExitLayout({ children }: { children: React.ReactNode }) {
  return children;
}
